services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: aqlayacake:latest
    container_name: aqlayacake_app
    restart: unless-stopped
    working_dir: /var/www
    env_file:
      - .env
    environment:
      DB_DATABASE: ${DB_DATABASE:-/var/www/storage/app/database.sqlite}
      RUN_MIGRATIONS: ${RUN_MIGRATIONS:-true}
      RUN_OPTIMIZE: ${RUN_OPTIMIZE:-true}
      MIGRATION_MAX_ATTEMPTS: ${MIGRATION_MAX_ATTEMPTS:-12}
      MIGRATION_RETRY_DELAY: ${MIGRATION_RETRY_DELAY:-5}
    ports:
      - "${APP_PORT:-8080}:80"
    volumes:
      - aqlayacake_storage:/var/www/storage
    networks:
      - web_network
    extra_hosts:
      - "host.docker.internal:host-gateway"
    mem_limit: 512m

volumes:
  aqlayacake_storage:

networks:
  web_network:
    name: ${DOCKER_WEB_NETWORK:-web_network}
    external: ${DOCKER_USE_EXTERNAL_NETWORK:-false}
